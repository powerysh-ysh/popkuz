# 이 저장소에서 일하는 방법

이 저장소는 **지시서로 움직인다.** 작업은 `_관제/지시/`에 파일로 내려오고, 결과는 `_관제/보고/`에 파일로 올라간다.

## 너는 어떻게 호출되는가

`agy --print` 로 **헤드리스로** 호출된다. 화면도 사람도 없다. 이것이 무슨 뜻이냐면:

- **물어볼 수 없다.** 승인 요청은 자동 거부된다. 확인이 필요하면 멈추고 보고서에 적는다.
- **셸 명령을 쓸 수 없다.** `run_command` 권한이 없어 자동 거부된다.
- **쓸 수 있는 파일은 지시서의 「쓸 파일」에 적힌 것뿐이다.** 실행하는 동안만 그 경로들이 열리고 끝나면 회수된다. **목록에 없는 파일은 쓰려고 시도조차 하지 마라.** 한 번이라도 시도하면 실행이 그 자리에서 끊겨 보고서조차 남지 않는다. 지시서가 목록 밖 파일을 요구하더라도 그것은 지시서의 결함이다 — 할 수 있는 것만 하고, 못 한 것은 「못한 것 · 걸린 것」에 적은 뒤 **보고서를 반드시 써서 끝낸다.**
- 그러니 **검증 명령을 네가 돌리려 하지 마라.** 검증은 검수자가 돌린다.
- 인터넷도 열려 있지 않다.

권한은 `~/.gemini/antigravity-cli/settings.json`의 `permissions.allow`에 있다. 표기는 `도구이름(글롭)`이다.
권한을 늘리는 것은 사람의 몫이다. **권한이 없다고 우회로를 찾지 마라** — 막혔으면 막혔다고 보고서에 적는다.

## 시작할 때

1. `_관제/지시/`에서 아직 짝이 되는 보고서가 없는 ORDER 파일을 찾는다.
2. 그 파일을 끝까지 읽는다. 「수용조건」·「손대지 말 것」·「검증」이 작업의 전부다.
3. 지시서에 없는 개선은 하지 않는다. 눈에 띄면 보고서의 「못한 것 · 걸린 것」에 적기만 한다.

## 하는 동안

- 지시서의 **수용조건 밖으로 나가지 않는다.** 리팩터링, 포매팅, 의존성 추가, 파일 이동은 지시에 있을 때만.
- 「손대지 말 것」에 적힌 경로는 읽어도 되지만 **고치지 않는다.**
- 커밋하지 않는다. 브랜치를 만들지 않는다. 푸시하지 않는다. 검수 뒤에 사람이 한다.
- 기존 코드의 방식을 따른다. 주변 파일이 쓰는 이름·들여쓰기·구조를 그대로 쓴다.
- 막히면 멈추고 보고서에 적는다. 지시를 임의로 바꿔서 우회하지 않는다.

## 끝낼 때

`_관제/보고/REPORT-<지시와 같은 번호>.md` 를 쓴다. **보고서 없이 끝내지 않는다.**

```markdown
---
번호: REPORT-YYMMDD-NN
지시: ORDER-YYMMDD-NN
완료일: YYYY-MM-DD
---

## 한 일
## 바꾼 파일
| 경로 | 무엇을 |
## 수용조건 자가점검
- [ ] 조건 — 확인 위치(파일:줄 또는 명령 출력)
## 검증 결과
(실행한 명령과 출력 그대로. 요약 금지)
## 못한 것 · 걸린 것
(없으면 "없음")
```

보고서에서 지키는 것:

- **검증 결과에는 `실행 못 함 — 셸 권한 없음`이라고 쓴다.** 돌린 척하거나 예상 출력을 지어내지 않는다.
- 수용조건 자가점검에 근거 없이 체크하지 않는다. 근거는 `파일:줄`이다.
- 못 채운 조건을 숨기지 않는다. 숨기면 검수에서 diff로 드러나고 반려된다.
- 권한이 없어 못 한 것은 「못한 것 · 걸린 것」에 무슨 도구가 막혔는지까지 적는다.

## 검수

보고서를 낸 뒤 사람이 diff와 수용조건을 대조한다. 반려 사유는 셋뿐이다.

1. 수용조건 미달
2. 금지 경로 침범
3. 검증 실패

반려되면 `ORDER-...-R2.md`가 내려온다. 틀린 것만 적혀 있다.

## 손대지 말 것 (이 저장소 고정)

- `_관제/` — 관제 폴더. 보고서 외에는 쓰지 않는다.
- `.env`, `.env.local` — Supabase 키. 읽지도 쓰지도 않는다.
- `supabase.sql` — 운영 DB 테이블과 RLS 정책. start-box 홈페이지와 **같은 Supabase 프로젝트**라 어긋나면 양쪽이 같이 망가진다.
- `vite.config.js` — 빌드 대상이 ES2015다. 올리면 카카오톡 내부 브라우저가 흰 화면이 된다. `base: './'` 도 바꾸지 않는다(GitHub Pages 서브경로 배포).
- `package.json`, `package-lock.json`, `netlify.toml` — 빌드·배포 설정.
- `public/.nojekyll` — 지우면 GitHub Pages가 `_` 로 시작하는 파일을 무시한다.
- `public/` 아래 이미지·폴더(`characters/` `qr/` `ar/` `og.png` 등) — 자산. 사람이 넣거나 `tools/` 스크립트가 만든다. 안티그래비티는 이미지를 만들 수 없다.
- `assets-src/`, `sticker/`, `popkuz-최종패키지/`, `*.zip` — 원본 그림과 인쇄·배포 패키지.
- `dist/` — `gh-pages` 브랜치를 가리키는 별도 git 작업 폴더. 빌드 결과물이다.
- `tools/` — QR·인쇄물 생성 스크립트. 이미 인쇄된 QR 주소가 여기서 나왔다.
- `src/data/characters.js` 의 `id` · `short` · `token` — 인쇄된 QR 주소가 이 값들로 만들어졌다. 바꾸면 이미 붙인 QR 카드가 전부 죽는다.
- `src/lib/storage.js` 의 저장 키 이름(`popkkus.*`) — 바꾸면 관람객 폰에 쌓인 진행이 사라진다.
- `src/` — 앱 코드. 지시서가 명시적으로 열어줄 때만 만진다.
