# 인수인계 — 팝꾸즈를 찾아라

최종 갱신 2026.09.26

## 1. 저장소

| | |
| --- | --- |
| 저장소 | https://github.com/powerysh-ysh/popkuz |
| `main` | 소스 코드 |
| `gh-pages` | 빌드 결과물(`dist/`)만 들어갑니다. **직접 수정하지 마세요** |
| 배포 주소 | https://powerysh-ysh.github.io/popkuz/ |
| 내 PC 경로 | `D:\code\2026-expo\popkuz` |

`dist/` 폴더가 `gh-pages` 브랜치를 가리키는 **별도의 git 작업 폴더**입니다.
`main` 저장소 안에 또 하나의 저장소가 들어 있는 구조라고 보시면 됩니다.

## 2. 시작하기

```
git clone https://github.com/powerysh-ysh/popkuz.git
cd popkuz
npm install
npm run dev
```

Node 20 이상이면 됩니다 (개발에는 24.19.0 / npm 11.17.0 을 썼습니다).

`npm run dev` 는 http://localhost:5173 에서 뜹니다.
**카메라는 `localhost` 이거나 `https` 여야 열립니다.** 다른 PC에서 접속해 테스트하려면
`npm run dev -- --host` 로 켜도 카메라는 막히니, 폰 테스트는 배포본으로 하세요.

## 3. 환경변수 (선택)

```
VITE_SUPABASE_URL=
VITE_SUPABASE_KEY=
```

**비워두어도 게임은 100% 작동합니다.** 진행 상황은 전부 브라우저(localStorage)에
저장되고, Supabase 는 참여 통계 집계용입니다.

- 형식은 `.env.example` 에 있습니다. 쓰려면 `.env` 로 복사해서 채우세요.
- 실제 값은 Supabase 대시보드 → Project Settings → API 에서 가져옵니다.
  https://supabase.com/dashboard/project/uakmfezejpzghqyfsdci
- 키는 **publishable** 키라 브라우저에 노출되는 것이 정상입니다.
  실제 보호는 RLS 정책이 합니다 — 새 프로젝트를 쓴다면 `supabase.sql` 을 반드시 실행하세요.
- `.env` 는 `.gitignore` 에 들어 있습니다. 커밋하지 마세요.

## 4. npm 명령

| 명령 | 하는 일 |
| --- | --- |
| `npm run dev` | 개발 서버 |
| `npm run build` | 이미지 변환 후 `dist/` 로 빌드 |
| `npm run preview` | 빌드 결과 미리보기 |
| `npm run images` | `assets-src/` 의 PNG → `public/characters/` 의 webp (배경 자동 제거) |
| `npm run qr -- <주소>` | 캐릭터 QR 카드 5종 + 입구 POP + 미니 라벨 |
| `npm run joinqr -- <주소>` | **모드별 참가 QR** (엑스포용 / 팝업스토어용) |
| `npm run stickers` | 스티커 인쇄 시안 |
| `npm run team` | 팀 설명서 |
| `npm run guide` | 게임 설명서 |
| `npm run ar` | AR용 GLB / USDZ 생성 |

`<주소>` 자리에는 `https://powerysh-ysh.github.io/popkuz` 를 넣습니다.

> **인쇄물(`print/`)은 저장소에 없습니다.** `.gitignore` 에 있어서 위 명령으로
> 다시 만들어야 합니다. 클론 직후 `npm run qr` · `npm run joinqr` 를 한 번씩 돌리세요.

## 5. 배포

```
npm run build
cd dist
git add -A
git commit -m "수정"
git push -f origin gh-pages
```

1~2분 뒤 같은 주소에 반영됩니다. **QR 카드는 다시 인쇄할 필요 없습니다.**

클론을 새로 했다면 `dist/` 작업 폴더를 한 번 만들어야 합니다.

```
npm run build
cd dist
git init
git remote add origin https://github.com/powerysh-ysh/popkuz.git
git checkout -b gh-pages
```

## 6. 조정하고 싶을 만한 숫자들

바꾸면 바로 난이도가 달라지는 값만 모았습니다.

### 진화·조각 — `src/lib/pieces.js`

| 값 | 현재 | 뜻 |
| --- | --- | --- |
| `EVOLVE_COST` | 6 | 진화에 필요한 조각 |
| `GAIN.qr` | 3 | QR로 처음 만났을 때 조각 |
| `GAIN.wild` | 2 | 야생으로 잡았을 때 조각 |

### 야생 출현 — `src/lib/wild.js`

| 값 | 현재 | 뜻 |
| --- | --- | --- |
| `MIN_GAP` / `MAX_GAP` | 4500 / 9000 | 다음 출현까지 대기 (ms) |
| `ESCAPE_MS` | 5000 | 못 잡으면 도망가는 시간 (ms) |

### 돌아다니기 — `src/lib/roam.js`

| 값 | 현재 | 뜻 |
| --- | --- | --- |
| `BASE_SPEED` | 3.1 | 기본 속도 (프레임당 px) |
| `SPEED_PER_MISS` | 0.8 | 빗나갈 때마다 빨라지는 폭 |
| `MAX_SPEED` | 8 | 속도 상한 |
| `PAUSE_MS` | [90, 320] | 도착 후 멈칫하는 시간 |
| `SWERVE_MS` | [600, 1400] | 도착 전에 방향을 트는 주기 |
| `bounds()` | ±96 / ±106px | 돌아다니는 범위 |

### 가품 — `src/lib/fake.js`

| 값 | 현재 | 뜻 |
| --- | --- | --- |
| `fakeChance` | `0.18 + 잡은수×0.025`, 최대 `0.5` | 가품으로 나올 확률 |
| `TELLS` | mirror · pale · blur · tilt | 단서 4종 |

> 단서를 추가할 때 **색조(hue)와 크기는 쓰지 마세요.** 반짝 개체가 색을 바꾸고
> 꼬마·특대 개체가 크기를 바꿔서, 진짜를 가짜로 오해하게 됩니다.

### 개체 변이 — `src/lib/variant.js`

| 값 | 현재 | 뜻 |
| --- | --- | --- |
| `EVO_CHANCE` | 0.14 | 진화형 개체 확률 |
| `SHINY_CHANCE` | 0.05 | 반짝 개체 확률 |
| `SIZES[].weight` | 12 / 76 / 12 | 꼬마 / 보통 / 특대 비율 |
| `SIZES[].scale` | 0.66 / 1 / 1.3 | 화면에 보이는 배율 |

### 맞추기 난이도

| 값 | 위치 | 현재 | 뜻 |
| --- | --- | --- | --- |
| `size={168}` | `src/pages/Scan.jsx` | 168 | 캐릭터 크기(px). **줄이면 어려워집니다** |
| `pad = t.width * 0.16` | `src/components/BallThrow.jsx` | 0.16 | 명중 판정 여유. **올리면 어려워집니다** |

> 명중 판정은 **캐릭터 그림**(`.tell`)에 걸려 있습니다. 바깥 상자(`.scan-target`)에
> 걸면 캐릭터를 줄여도 판정 범위가 그대로라 너무 잘 맞습니다. 한 번 그렇게
> 만들었다가 고쳤으니 되돌리지 마세요.

### 중간 미션 — `src/lib/quest.js`

`QUESTS` 배열에 `{ id, ev, goal, rare, text }` 를 추가하면 바로 돌아갑니다.
`ev` 는 `wild` · `new` · `ball` · `streak` 중 하나입니다.

### 모드 설정 — `src/lib/mode.js`

`MODES.expo` / `MODES.store` 에서 기능을 켜고 끕니다
(`hasCoupon` · `hasWild` · `hasMission` · `sequential`).
`STORE_PLAN` 에 팝업스토어 캐릭터별 난이도와 할인권 금액이 있습니다.

### 캐릭터 — `src/data/characters.js`

이름·속성·대사·색·짧은 코드·진화형 이름이 전부 여기 있습니다.
**캐릭터를 추가하려면** 여기에 항목을 넣고, `assets-src/` 에 기본·진화형 PNG를
넣은 뒤 `npm run images` → `npm run qr` 을 돌리면 됩니다.

## 7. 화면 주소

`#` 이 없는 주소와 있는 주소 둘 다 됩니다.

| 용도 | 주소 |
| --- | --- |
| 엑스포 모드로 시작 | `/popkuz/expo` |
| 팝업스토어 모드로 시작 | `/popkuz/store` |
| 스태프 (모드 전환 · 초기화 · 할인권 처리) | `/popkuz/staff` |
| 장소 설정 | `/popkuz/setup` |
| 화면 QR 보드 | `/popkuz/board` |
| 내 할인권 | `/popkuz/wallet` |

### 시연·점검용

| 주소 | 하는 일 |
| --- | --- |
| `#/scan?debug=1` | 인식 엔진·프레임·인식 수 표시 |
| `#/scan?demo=chokku` | 카메라 없이 발견 연출 |
| `#/scan?demo=chokku&wild=1` | 야생으로 표시 (「가짜 같은데?」 버튼) |
| `#/scan?demo=chokku&wild=1&fake=mirror` | 가품 (`mirror` `pale` `blur` `tilt`) |
| `#/scan?demo=chokku&wild=1&shiny=1&evo=1&size=xxl` | 반짝·진화형·크기 개체 |

## 8. 저장되는 곳 (전부 localStorage)

| 키 | 내용 |
| --- | --- |
| `popkkus.v1` | 도감·닉네임·완주 |
| `popkkus.pieces.v1` | 조각·반짝조각·진화 |
| `popkkus.quest.v1` | 진행 중인 중간 미션 |
| `popkkus.wild.v1` | 오늘 잡은 야생 수 |
| `popkkus.coupons.v1` | 할인권 (팝업스토어) |
| `popkkus.mission.v1` / `popkkus.best.v1` | 스피드 미션·최고 기록 |
| `popkkus.mode.v1` | 운영 모드 (초기화해도 안 지워짐) |
| `popkkus.spots.v1` | 장소 설정 (초기화해도 안 지워짐) |

스태프 화면의 **「진행 초기화」**가 위쪽 여섯 개를 지웁니다.
모드와 장소는 스태프가 정한 값이라 남깁니다 — `src/lib/storage.js` 의 `reset()`.

## 9. 알아두면 시간 아끼는 것들

- **`requestAnimationFrame` 을 QR 스캔 루프에 쓰지 마세요.** 화면이 안 보이면
  멈춰서 스캔이 조용히 죽습니다. `setInterval` 로 되어 있습니다 (`src/lib/scanner.js`).
- **useEffect 의존성 배열은 렌더 중에 평가됩니다.** 배열에 쓰는 변수는 반드시
  그 위에서 선언하세요. 한 번 이것 때문에 탐지기 화면이 통째로 죽었습니다.
- **빌드 대상이 ES2015 입니다** (`vite.config.js`). 카카오톡 내부 브라우저가
  최신 문법을 못 읽어서 흰 화면이 됐습니다. 올리지 마세요.
- **`public/.nojekyll` 을 지우지 마세요.** GitHub Pages 가 `_` 로 시작하는
  파일을 무시합니다.
- **`base: './'`** 이고 HashRouter 입니다. 서브경로 배포라 절대경로를 쓰면 깨집니다.
- **Supabase 요청에 `Prefer: resolution=...` 을 넣지 마세요.** PostgREST 가
  UPSERT 로 취급해서 UPDATE 정책까지 요구합니다. 통계가 통째로 안 쌓였습니다.

## 10. 아직 안 된 것

- 실제 폰 카메라로 종이 QR 읽기 검증 (개발 환경에 카메라가 없음)
- 조각·진화·미션·가품 기록은 **서버로 안 보냅니다.** 폰 안에서만 돌아갑니다
- 1만원 할인권 하루 수량 제한 자동화 (현재 운영으로만 관리)
- 팝업스토어 단서·수수께끼 문구 (`STORE_PLAN` 에 데이터는 있고 화면에 안 씀)
- 인쇄물 `10-게임설명서` · `06-입구POP` 은 조각·진화·가품이 들어가기 전 버전
